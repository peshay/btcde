checks:
    python:
        code_rating: true
        duplicate_code: true

filter:
    excluded_paths:
        - tools/*

build:
  nodes:
    analysis:
      tests:
        override:
          - py-scrutinizer-run
  environment:
    python:
      version: "3.11.3"
      virtualenv: true